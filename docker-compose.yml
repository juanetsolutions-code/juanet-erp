services:
  # PHP Application Container (Laravel 12 / PHP 8.4-FPM)
  app:
    build:
      context: .
      dockerfile: Dockerfile
      target: dev
    image: juanet-app:dev
    container_name: juanet-app
    restart: unless-stopped
    working_dir: /var/www/html
    extra_hosts:
      - "host.docker.internal:host-gateway"
    environment:
      APP_ENV: local
      APP_DEBUG: "true"
      APP_KEY: base64:JUANETEnterpriseSaaSPlatformKeyLocalDev84=
      APP_URL: http://localhost:8080
      DB_CONNECTION: pgsql
      DB_HOST: host.docker.internal # Connects to Supabase or host PostgreSQL
      DB_PORT: 5432
      DB_DATABASE: postgres
      DB_USERNAME: postgres
      DB_PASSWORD: local_dev_password
      REDIS_HOST: redis
      REDIS_PORT: 6379
      QUEUE_CONNECTION: redis
      CACHE_STORE: redis
      SESSION_DRIVER: redis
      FILESYSTEM_DISK: s3
      AWS_ACCESS_KEY_ID: juanet_local_key
      AWS_SECRET_ACCESS_KEY: juanet_local_secret
      AWS_DEFAULT_REGION: us-east-1
      AWS_BUCKET: juanet-local-bucket
      AWS_USE_PATH_STYLE_ENDPOINT: "true"
      AWS_ENDPOINT: http://minio:9000
      MAIL_MAILER: smtp
      MAIL_HOST: mailpit
      MAIL_PORT: 1025
      MAIL_ENCRYPTION: null
    volumes:
      - .:/var/www/html:delegated
      - ./docker/php/php.ini:/usr/local/etc/php/conf.d/99-custom.ini:ro
    networks:
      - juanet-network
    depends_on:
      - redis
      - minio

  # Queue Worker Container
  queue-worker:
    build:
      context: .
      dockerfile: Dockerfile
      target: dev
    image: juanet-app:dev
    container_name: juanet-queue-worker
    restart: unless-stopped
    working_dir: /var/www/html
    command: php artisan queue:work --queue=default,high,low --tries=3 --timeout=60
    volumes:
      - .:/var/www/html:delegated
    networks:
      - juanet-network
    depends_on:
      - app
      - redis

  # Cron Scheduler Container
  scheduler:
    build:
      context: .
      dockerfile: Dockerfile
      target: dev
    image: juanet-app:dev
    container_name: juanet-scheduler
    restart: unless-stopped
    working_dir: /var/www/html
    command: php artisan schedule:work
    volumes:
      - .:/var/www/html:delegated
    networks:
      - juanet-network
    depends_on:
      - app
      - redis

  # Web Server (Nginx)
  web:
    image: nginx:1.27-alpine
    container_name: juanet-web
    restart: unless-stopped
    ports:
      - "8080:80"
    volumes:
      - .:/var/www/html:delegated
      - ./docker/nginx/conf.d:/etc/nginx/conf.d:ro
    networks:
      - juanet-network
    depends_on:
      - app

  # Redis (Cache, Session, Queue Store)
  redis:
    image: redis:7.2-alpine
    container_name: juanet-redis
    restart: unless-stopped
    ports:
      - "6379:6379"
    volumes:
      - juanet-redis-data:/data
      - ./docker/redis/redis.conf:/usr/local/etc/redis/redis.conf:ro
    command: redis-server /usr/local/etc/redis/redis.conf
    networks:
      - juanet-network

  # MinIO (S3 Compatible Local Object Storage)
  minio:
    image: minio/minio:RELEASE.2024-06-06T09-36-42Z
    container_name: juanet-minio
    restart: unless-stopped
    ports:
      - "9000:9000" # S3 API port
      - "9001:9001" # Web Console port
    environment:
      MINIO_ROOT_USER: juanet_local_key
      MINIO_ROOT_PASSWORD: juanet_local_secret
    volumes:
      - juanet-minio-data:/data
    command: minio server /data --console-address ":9001"
    networks:
      - juanet-network

  # MinIO Auto Bucket Creation Utility
  minio-create-buckets:
    image: minio/mc:RELEASE.2024-06-05T20-19-48Z
    container_name: juanet-minio-provisioner
    depends_on:
      - minio
    entrypoint: >
      /bin/sh -c "
      until (/usr/bin/mc alias set localminio http://minio:9000 juanet_local_key juanet_local_secret) do echo 'Waiting for MinIO...' && sleep 1; done;
      /usr/bin/mc mb --ignore-existing localminio/juanet-local-bucket;
      /usr/bin/mc anonymous set public localminio/juanet-local-bucket;
      exit 0;
      "
    networks:
      - juanet-network

  # Mailpit (SMTP Mock Server and Mail GUI)
  mailpit:
    image: axllent/mailpit:v1.18
    container_name: juanet-mailpit
    restart: unless-stopped
    ports:
      - "1025:1025" # SMTP server
      - "8025:8025" # Web UI
    networks:
      - juanet-network

networks:
  juanet-network:
    driver: bridge

volumes:
  juanet-redis-data:
    driver: local
  juanet-minio-data:
    driver: local
